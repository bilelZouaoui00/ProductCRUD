
first install :
    npm init -y --initialisatino
    npm i express mysql nodemon --installation

create the main javascript file :
    index.js
    //code for initial lissner 
    add after the main in the package.json --> "type": "module",

run the backend : 
     node index.js

Details :
for the auto saving we add in 'package.json' in 'scripts'
    "start": "nodemon index.js"
    the we start the server with "npm start"

To allow front end application use the API from the server
    npm i cors

install axios library in server side for api request
    npm i axios
insall a cors library to allow front end application use
    npm i cors